pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r ./index.html .public
  - cp -r ./js/* .public
  - cp -r ./audio/* .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
